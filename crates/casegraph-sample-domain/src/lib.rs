#![forbid(unsafe_code)]

//! Artificial sample package used only to prove domain extension isolation.

/// Stable package identifier; this is deliberately not a production vertical.
pub const PACKAGE_ID: &str = "sample-administrative-case";
